// Generated by CoffeeScript 1.3.3
(function() {
  var MockWebSocket,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  MockWebSocket = (function() {

    function MockWebSocket(uri) {
      this.receive = __bind(this.receive, this);

      this.send = __bind(this.send, this);
      MockWebSocket.lastCreated = this;
    }

    MockWebSocket.prototype.send = function(message) {
      return this.lastMessageSent = message;
    };

    MockWebSocket.prototype.receive = function(message) {
      if (this.onmessage) {
        return this.onmessage.call(this, message);
      }
    };

    return MockWebSocket;

  })();

  module.exports = MockWebSocket;

  global.WebSocket = MockWebSocket;

}).call(this);
